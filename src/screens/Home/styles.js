import { green } from 'ansi-colors';

const styles = {
  container: {
    marginLeft: 40,
    marginRight: 40,
    alignItems: 'center',
    justifyContent: 'center'
  },
  coverImage: {
    height: '58px',
    width: '38px',
  },
  selectedRow: {
    background: green,
  }
};

export default styles;
